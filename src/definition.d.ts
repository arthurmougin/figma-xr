export enum logStateOptions {
    'not logged in',
    'logging in',
    'logged in',
    'error'
}

export type ProfileType = {
    id: string,
    email: string,
    handle: string,
    img_url: string
}

